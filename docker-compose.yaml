version: '3.8'
services:
  postgres:
    image: postgres:15
    container_name: profitly-postgres
    restart: always
    environment:
      POSTGRES_USER: profitly_user
      POSTGRES_PASSWORD: profitly_pass
      POSTGRES_DB: profitly_db
      TZ: Asia/Kolkata
      POSTGRES_INITDB_ARGS: "--timezone=Asia/Kolkata"
    ports:
      - "5432:5432"
    volumes:
      - profitly_pgdata:/var/lib/postgresql/data

  zookeeper:
    image: confluentinc/cp-zookeeper:7.5.0
    container_name: zookeeper
    environment:
      ZOOKEEPER_CLIENT_PORT: 2181
      ZOOKEEPER_TICK_TIME: 2000

  kafka:
    image: confluentinc/cp-kafka:7.5.0
    container_name: kafka
    depends_on:
      - zookeeper
    ports:
      - "9092:9092"
    environment:
      KAFKA_BROKER_ID: 1
      KAFKA_ZOOKEEPER_CONNECT: zookeeper:2181
      # These settings allow you to connect to Kafka from your machine (localhost)
      KAFKA_ADVERTISED_LISTENERS: PLAINTEXT://kafka:29092,PLAINTEXT_HOST://localhost:9092
      KAFKA_LISTENER_SECURITY_PROTOCOL_MAP: PLAINTEXT:PLAINTEXT,PLAINTEXT_HOST:PLAINTEXT
      KAFKA_INTER_BROKER_LISTENER_NAME: PLAINTEXT
      KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR: 1

volumes:
  profitly_pgdata: